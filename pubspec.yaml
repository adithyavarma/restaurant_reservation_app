name: reservation_app
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1
environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^6.0.0
  provider: ^6.0.5            #state management
  http: ^1.1.0                #for API calls
  table_calendar: ^3.0.9      #calendar UI
  firebase_core: ^4.1.0
  firebase_auth: ^6.0.2      #firebase authentication
  firebase_auth_web: ^6.0.2
  cloud_firestore: ^6.0.1     #firestore database
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
